{
  "name": "holder-ar-webapp-tools",
  "private": true,
  "version": "0.0.1",
  "description": "Build-time tools for holder-ar webapp (frame compression etc.)",
  "scripts": {
    "build:frames": "node build-frames.cjs"
  },
  "dependencies": {
    "sharp": "^0.33.0"
  }
}
